import { Cookie, Indie_Flower, Delius } from "next/font/google";

export const cookie = Cookie({
  subsets: ["latin"],
  weight: "400",
});

export const indieFlower = Indie_Flower({
  subsets: ["latin"],
  weight: "400",
});

export const delius = Delius({
  subsets: ["latin"],
  weight: "400",
});
